apiVersion: 1

providers:
  - name: CuisineEnsemble
    orgId: 1
    folder: CuisineEnsemble
    folderUid: cuisine-ensemble
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
